# Hot AI — Art Assets Requirements

This document is the contract between engineering and design. Assets marked required are
referenced by runtime metadata or the critical path; optional assets are reserved for future
surfaces and should still follow the same visual system. Missing assets degrade gracefully
where inline SVG or CSS fallbacks exist.

> **Conventions**
> - Drop files into `apps/web/public/` at the paths shown.
> - Prefer SVG for iconography; keep the raster family reproducible from `scripts/generate_signal_assets.py`.
> - The visual source of truth is `apps/web/app/globals.css`; Tailwind mirrors the same tokens for utility classes.

---

## 1 · Brand Palette (LAMDA briefing)

| Token          | Hex       | Use                                      |
| -------------- | --------- | ---------------------------------------- |
| `--bg`         | `#f4f0e8` | Quiet paper background                   |
| `--card`       | `#fffdf8` | Editorial cards and input surfaces       |
| `--ink`        | `#27231f` | Typography, rules, primary structure     |
| `--vermillion` | `#b4473f` | Restrained editorial accent              |
| `--signal-red` | `#b4473f` | Alerts and small reading markers         |
| `--blue`       | `#365d70` | Focus and secondary contrast             |
| `--mild`       | `#6f675e` | Secondary copy and metadata              |

The palette is intentionally flat and reproducible: no ember-to-fuchsia fire gradient is
part of the current brand system.

---

## 2 · Logo & favicons  ★ Required

| File                          | Size               | Format | Code references                                |
| ----------------------------- | ------------------ | ------ | ---------------------------------------------- |
| `public/logo.svg`             | 64×64, viewBox 0 0 64 64 | SVG  | Canonical vector source; fallback/reference asset |
| `public/icon.svg`             | 32×32 viewBox      | SVG    | `app/layout.tsx` (`icons.icon`)                |
| `public/favicon.ico`          | 16+32+48 multi-res | ICO    | Browser shortcut                               |
| `public/apple-touch-icon.png` | 180×180            | PNG    | iOS home-screen                                |
| `public/icon-192.png`         | 192×192            | PNG    | PWA-style installs                             |
| `public/icon-512.png`         | 512×512            | PNG    | PWA / share preview                            |

**Design brief.** LAMDA uses a paper ground, ink typography, a restrained vermillion mark,
and a quiet geometric signal motif. Keep the mark square and legible at 16×16; do not
reintroduce yellow blocks, hard color shadows, or rounded fire gradients. `public/logo.svg`
and `public/icon.svg` are the canonical vector sources. The PNG/ICO family is generated by
`scripts/generate_signal_assets.py`.

---

## 3 · Open Graph / Twitter card  ★ Required

| File              | Size      | Format | Code references                             |
| ----------------- | --------- | ------ | ------------------------------------------- |
| `public/og.png`   | 1200×630  | PNG    | English variant; available for route-level metadata |
| `public/og-zh.png`| 1200×630  | PNG    | `app/layout.tsx` default Open Graph/Twitter image |

**Design brief.** The briefing cards use a paper field, ink typography, and a small vermillion
editorial marker. Keep the safe area ≥ 80px from edges and retain the large editorial
hierarchy. `og.png` is the English variant; `og-zh.png` is the Chinese-first default used by
`app/layout.tsx`.

Keep the safe area ≥ 80px from edges (LinkedIn / Slack crop). Export at 1× only.

---

## 4 · Hero illustration  ◇ Nice-to-have

| File                       | Size      | Format | Code references                         |
| -------------------------- | --------- | ------ | --------------------------------------- |
| `public/hero-spark.svg`    | ~480×360  | SVG    | Optional asset; no current runtime mount |

**Design brief.** If this illustration is brought back into the UI, use the LAMDA motif:
sparse registration dots resolving into bars, paper ground, ink typography, and one small
vermillion marker. Avoid the retired ember/fuchsia gradient.

If shipped, mount it from the actual feed masthead (`components/NookFeed.tsx`) and add a
responsive rule there. The repository has no `components/Hero.tsx`; do not describe this
asset as visible until a real component imports it.

---

## 5 · Source favicons / fallback  ✓ Fallback implemented

The article card currently fetches favicons via `https://www.google.com/s2/favicons` —
this is fast but uses a third-party endpoint and can be blocked in some regions.

| File                          | Size  | Format | Use                                                |
| ----------------------------- | ----- | ------ | -------------------------------------------------- |
| `public/source-fallback.svg`  | 16×16 | SVG    | Shown when Google's favicon endpoint fails / blocked |
| `public/sources/{slug}.svg`   | 16×16 | SVG    | Optional: ship our own icons for top-20 sources to skip the network round-trip entirely |

**Design brief.** Fallback should be a neutral monochrome glyph (a small "○" with the
first letter of the source name, generated server-side from the source name) — see
implementation note in `components/ArticleCard.tsx`. Per-source icons should be the
host's actual logo, hand-traced to 16×16 grid, monochrome where possible to survive both
light and dark themes.

---

## 6 · Category icons  ✓ Implemented and runtime-linked

The four category icons are mounted by `components/CategoryNav.tsx` as CSS masks, so they
inherit the current foreground colour in both themes.

| File                                 | Size   | Format | Reference                            |
| ------------------------------------ | ------ | ------ | ------------------------------------ |
| `public/categories/research.svg`     | 20×20  | SVG    | `components/CategoryNav.tsx`         |
| `public/categories/industry.svg`     | 20×20  | SVG    | (would render before the label)      |
| `public/categories/opensource.svg`   | 20×20  | SVG    |                                      |
| `public/categories/media.svg`        | 20×20  | SVG    |                                      |

**Design brief.** Single-stroke line icons (1.5px), 20×20 grid. Suggestions:
- research — a stylised "beaker" or "paper with axis lines"
- industry — a "circuit / chip" outline
- opensource — a "fork in the road" or "branched node"
- media — a "broadcasting tower" or "speech bubble with a dot"

Match Heroicons' visual weight (we don't import Heroicons — keep it custom).

---

## 7 · Sentiment glyphs  ✓ Implemented and runtime-linked

`ArticleCard` mounts these glyphs when AI assigns a sentiment label (`release`, `research`,
`opinion`, `rumor`, `tutorial`, `other`). They use CSS masks and inherit chip colour.

| File                                | Size   | Format | Each label                                 |
| ----------------------------------- | ------ | ------ | ------------------------------------------ |
| `public/sentiment/release.svg`      | 14×14  | SVG    | Rocket / package                           |
| `public/sentiment/research.svg`     | 14×14  | SVG    | Telescope / lens                           |
| `public/sentiment/opinion.svg`      | 14×14  | SVG    | Speech bubble                              |
| `public/sentiment/rumor.svg`        | 14×14  | SVG    | Question mark / whisper                    |
| `public/sentiment/tutorial.svg`     | 14×14  | SVG    | Book / play                                |
| `public/sentiment/other.svg`        | 14×14  | SVG    | Dot                                        |

Use `currentColor` so the chip's text colour drives the glyph.

---

## 8 · 404 illustration  ✓ Implemented and runtime-linked

| File                       | Size      | Format | Reference                |
| -------------------------- | --------- | ------ | ------------------------ |
| `public/404-flameout.svg`  | ~320×240  | SVG    | `app/not-found.tsx`      |

**Design brief.** A quiet registration error: broken bars, a missing pulse, or a misaligned
plate drawn in the same paper/ink/vermillion language as the masthead. Keep it calm and
editorial rather than using an alarming flame illustration.

---

## 9 · Loading skeletons  — already in CSS

No assets needed; `globals.css` defines `.skeleton` with a shimmer animation. Designers
should be aware that all card placeholders use this class so the cadence/contrast can be
tuned in CSS without re-exporting.

---

## 10 · Out-of-scope (for now)

- Photographic / hero photography — the design intentionally avoids stock imagery.
- Per-article cover images — we don't extract OG images from source articles yet.
- Animated/Lottie assets — keep it CSS-only for performance and zero JS deps.

---

## Delivery checklist

- [x] `logo.svg` + raster icon family
- [x] `og.png` (1200×630)
- [x] `apple-touch-icon.png` (180×180)
- [x] `favicon.ico` (multi-res)
- [x] Source fallback glyph (`source-fallback.svg`)
- [x] At least one hero illustration variant (`hero-spark.svg`, optional runtime surface)
- [x] Category icon set (4)
- [x] Sentiment glyph set (6)
- [x] 404 illustration (`404-flameout.svg`, wired into `app/not-found.tsx`)

### Runtime wiring note

The manifest, icon family, and `og-zh.png` are runtime-linked today. `og.png` remains the
English export for future locale-specific metadata. `hero-spark.svg` is intentionally an
optional asset and is not mounted in the current feed masthead; do not describe it as a
visible hero until a component imports it. `public/sw.js` is a cleanup/unregister worker,
not an offline cache implementation, so the repository should not claim a complete offline
PWA based on the manifest alone.
